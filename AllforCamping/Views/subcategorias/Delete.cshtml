@model AllforCamping.Models.subcategorias

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div style="background-color: burlywood ">
    <div class="container-fluid h1 text-center">
        <h1>Eliminar subcategoria</h1>
    </div>
    <div class="container-fluid text-danger">

        <h3>¿Estas seguro que deseas eliminar esta subcategoria?</h3>
    </div>

    <div class="container-fluid">

        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.nombre)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.nombre)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.categoria)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.categoria)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.categorias.nombre)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.categorias.nombre)
            </dd>

        </dl>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color container-fluid">
                <input type="submit" value="Delete" class="btn btn-danger" />
                <button type="button" class="btn btn-default">@Html.ActionLink("Regresar", "Index")</button>
            </div>

            <br />
            <br />
            <br />
            <br />
        }
    </div>
</div>